@model IEnumerable<Test.Models.Expense>

@{
    ViewData["Title"] = "Expenses Dashboard";
}
<div class="container mt-5">
    <div class="card shadow p-4">
        <h2 class= "text-primary mb-3"> My Solo Budget Dash</h2>
        <p class="text-muted">Welcome to your own financial workspace!</p>

        <hr />

        <div class="alert alert-info text-center" role="alert">
            Here we feed database rows.
        </div>
    </div>
</div>